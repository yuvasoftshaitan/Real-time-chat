<nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
  <a class="navbar-brand btn btn-outline-primary" href="/pages/home">Home</a>
  <a class="navbar-brand btn btn-outline-primary" href="/users/index">Group List</a>
   <a class="navbar-brand btn btn-outline-primary" href="/users/usershow">User List</a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if current_user  %>
       <span class="mr-2 text-white"><%=link_to current_user.email , edit_user_registration_path(current_user), class: "btn btn-outline-primary text-light" %></span>
        
        <%end%>
      <li class="nav-item active">
        <% if current_user %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-warning" %>
        <% else %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-warning'  %> ||
            <%= link_to 'Login', new_user_session_path , class: "btn btn-warning"%>
        <% end %>
      </li>
    </ul>
    
  </div>
</nav>


